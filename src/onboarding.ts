import {Env,audit,authenticate,body,json} from './lib';

export async function handleOnboarding(request:Request,env:Env,url:URL):Promise<Response|null>{if(!url.pathname.startsWith('/api/onboarding'))return null;const actor=await authenticate(request,env);if(!actor)return json({error:'Sessão ausente ou expirada.'},401);if(actor.actorType!=='tenant_user'||!actor.tenantId)return json({error:'Onboarding disponível apenas para empresas autenticadas.'},403);if(!['admin'].includes(actor.role))return json({error:'Apenas o administrador da empresa pode concluir o onboarding.'},403);const tenantId=actor.tenantId;const method=request.method.toUpperCase();
if(method==='GET'&&url.pathname==='/api/onboarding'){const tenant=await env.DB.prepare(`SELECT t.id,t.slug,t.name,t.segment,t.status,s.public_name,s.legal_name,s.document,s.email,s.phone,s.whatsapp,s.primary_color,s.secondary_color,s.logo_key FROM tenants t LEFT JOIN tenant_settings s ON s.tenant_id=t.id WHERE t.id=? LIMIT 1`).bind(tenantId).first();const onboarding=await env.DB.prepare(`SELECT current_step,completed,completed_at,company_json,operations_json,channels_json,branding_json FROM tenant_onboarding WHERE tenant_id=? LIMIT 1`).bind(tenantId).first();return json({data:{tenant,onboarding:onboarding||{current_step:1,completed:0}}});}
if(method==='PUT'&&url.pathname==='/api/onboarding'){const input=await body(request);const company=(input.company&&typeof input.company==='object'?input.company:{}) as Record<string,unknown>;const operations=(input.operations&&typeof input.operations==='object'?input.operations:{}) as Record<string,unknown>;const channels=(input.channels&&typeof input.channels==='object'?input.channels:{}) as Record<string,unknown>;const branding=(input.branding&&typeof input.branding==='object'?input.branding:{}) as Record<string,unknown>;const currentStep=Math.min(5,Math.max(1,Number(input.current_step||1)));const completed=Boolean(input.completed);const publicName=String(company.public_name||company.name||'').trim();const segment=String(operations.segment||'').trim();if(publicName)await env.DB.prepare(`UPDATE tenant_settings SET public_name=?,legal_name=?,document=?,email=?,phone=?,whatsapp=?,primary_color=?,secondary_color=? WHERE tenant_id=?`).bind(publicName,company.legal_name?String(company.legal_name):null,company.document?String(company.document):null,company.email?String(company.email):null,company.phone?String(company.phone):null,channels.whatsapp?String(channels.whatsapp):null,branding.primary_color?String(branding.primary_color):'#169CFF',branding.secondary_color?String(branding.secondary_color):'#0B2B7C',tenantId).run();if(segment)await env.DB.prepare(`UPDATE tenants SET segment=? WHERE id=?`).bind(segment,tenantId).run();await env.DB.prepare(`INSERT INTO tenant_onboarding (tenant_id,current_step,completed,completed_at,company_json,operations_json,channels_json,branding_json,updated_at) VALUES (?,?,?,?,?,?,?,?,datetime('now')) ON CONFLICT(tenant_id) DO UPDATE SET current_step=excluded.current_step,completed=excluded.completed,completed_at=excluded.completed_at,company_json=excluded.company_json,operations_json=excluded.operations_json,channels_json=excluded.channels_json,branding_json=excluded.branding_json,updated_at=datetime('now')`).bind(tenantId,currentStep,completed?1:0,completed?new Date().toISOString():null,JSON.stringify(company),JSON.stringify(operations),JSON.stringify(channels),JSON.stringify(branding)).run();await audit(env,actor,tenantId,completed?'onboarding.completed':'onboarding.updated','tenant',tenantId,{current_step:currentStep});return json({data:{ok:true,current_step:currentStep,completed}});}
return json({error:'Endpoint não encontrado.'},404);}
