import {Env,audit,authenticate,body,cents,hasRole,json,makeId,resolveTenant} from './lib';
import {handleAuth} from './auth';

export async function handleApi(request:Request,env:Env,url:URL):Promise<Response|null>{if(!url.pathname.startsWith('/api/'))return null;const method=request.method.toUpperCase();if(method==='GET'&&url.pathname==='/api/health')return json({ok:true,app:'NegocIAJá!',version:'0.3.0',now:new Date().toISOString()});const authResponse=await handleAuth(request,env,url);if(authResponse)return authResponse;const actor=await authenticate(request,env);if(!actor)return json({error:'Sessão ausente ou expirada.'},401);const tenantId=await resolveTenant(request,env,actor);if(!tenantId)return json({error:actor.role==='super_admin'?'Selecione um tenant válido.':'Tenant da sessão inválido.'},400);
if(method==='GET'&&url.pathname==='/api/dashboard'){const[customers,catalog,openOrders,sales,conversations]=await Promise.all([env.DB.prepare('SELECT COUNT(*) total FROM customers WHERE tenant_id=?').bind(tenantId).first<{total:number}>(),env.DB.prepare('SELECT COUNT(*) total FROM catalog_items WHERE tenant_id=? AND active=1').bind(tenantId).first<{total:number}>(),env.DB.prepare("SELECT COUNT(*) total FROM orders WHERE tenant_id=? AND status NOT IN ('done','cancelled')").bind(tenantId).first<{total:number}>(),env.DB.prepare("SELECT COALESCE(SUM(total_cents),0) total FROM orders WHERE tenant_id=? AND status!='cancelled'").bind(tenantId).first<{total:number}>(),env.DB.prepare("SELECT COUNT(*) total FROM conversations WHERE tenant_id=? AND status IN ('ai','human')").bind(tenantId).first<{total:number}>()]);return json({data:{customers:customers?.total??0,catalogItems:catalog?.total??0,openOrders:openOrders?.total??0,salesCents:sales?.total??0,activeConversations:conversations?.total??0,session:{name:actor.name,role:actor.role,tenantId}}});}
if(method==='GET'&&url.pathname==='/api/catalog'){const result=await env.DB.prepare(`SELECT id,sku,name,description,item_type,category,unit,pricing_mode,price_cents,active,stock_control,stock_qty,attributes_json,options_json,image_key FROM catalog_items WHERE tenant_id=? AND active=1 ORDER BY category,name`).bind(tenantId).all();return json({data:result.results});}
if(method==='POST'&&url.pathname==='/api/catalog'){if(!hasRole(actor,['admin','super_admin']))return json({error:'Permissão insuficiente para alterar catálogo.'},403);const input=await body(request),name=String(input.name||'').trim();if(!name)return json({error:'Nome do item é obrigatório.'},400);const id=makeId('item');await env.DB.prepare(`INSERT INTO catalog_items (id,tenant_id,sku,name,description,item_type,category,unit,pricing_mode,price_cents,active,stock_control,stock_qty,attributes_json,options_json) VALUES (?,?,?,?,?,?,?,?,?,?,1,?,?,?,?)`).bind(id,tenantId,input.sku?String(input.sku):null,name,input.description?String(input.description):null,String(input.item_type||'product'),input.category?String(input.category):null,String(input.unit||'un'),String(input.pricing_mode||'fixed'),cents(input.price_cents),input.stock_control?1:0,Number(input.stock_qty||0),JSON.stringify(input.attributes||{}),JSON.stringify(input.options||[])).run();await audit(env,actor,tenantId,'catalog.create','catalog_item',id,{name});return json({data:{id,name}},201);}
const catalogRoute=url.pathname.match(/^\/api\/catalog\/([^/]+)$/);if(method==='PATCH'&&catalogRoute){if(!hasRole(actor,['admin','super_admin']))return json({error:'Permissão insuficiente para alterar catálogo.'},403);const input=await body(request),id=catalogRoute[1];const current=await env.DB.prepare(`SELECT id,name,item_type,category,price_cents,stock_control,stock_qty,active FROM catalog_items WHERE id=? AND tenant_id=? LIMIT 1`).bind(id,tenantId).first<{id:string;name:string;item_type:string;category:string|null;price_cents:number;stock_control:number;stock_qty:number;active:number}>();if(!current)return json({error:'Item não encontrado.'},404);const name=input.name===undefined?current.name:String(input.name||'').trim();if(!name)return json({error:'Nome do item é obrigatório.'},400);const itemType=input.item_type===undefined?current.item_type:String(input.item_type||'product');const category=input.category===undefined?current.category:(input.category?String(input.category):null);const priceCents=input.price_cents===undefined?current.price_cents:cents(input.price_cents);const stockControl=input.stock_control===undefined?current.stock_control:(input.stock_control?1:0);const stockQty=input.stock_qty===undefined?current.stock_qty:Math.max(0,Number(input.stock_qty||0));const active=input.active===undefined?current.active:(input.active?1:0);await env.DB.prepare(`UPDATE catalog_items SET name=?,item_type=?,category=?,price_cents=?,stock_control=?,stock_qty=?,active=?,updated_at=datetime('now') WHERE id=? AND tenant_id=?`).bind(name,itemType,category,priceCents,stockControl,stockQty,active,id,tenantId).run();await audit(env,actor,tenantId,'catalog.update','catalog_item',id,{active:Boolean(active)});return json({data:{id,name,active:Boolean(active)}});}
if(method==='GET'&&url.pathname==='/api/orders'){const result=await env.DB.prepare(`SELECT o.id,o.public_code,o.transaction_type,o.status,o.source,o.total_cents,o.payment_status,o.fulfillment_type,o.created_at,c.name customer_name,c.phone customer_phone FROM orders o LEFT JOIN customers c ON c.id=o.customer_id WHERE o.tenant_id=? ORDER BY o.created_at DESC LIMIT 100`).bind(tenantId).all();return json({data:result.results});}
if(method==='POST'&&url.pathname==='/api/orders'){if(!hasRole(actor,['operator','admin','super_admin']))return json({error:'Permissão insuficiente.'},403);const input=await body(request),lines=Array.isArray(input.items)?input.items as Array<Record<string,unknown>>:[];if(!lines.length)return json({error:'Adicione pelo menos um item.'},400);const customerName=String(input.customer_name||'Cliente').trim(),phone=input.customer_phone?String(input.customer_phone):null;let customerId:string|null=null;if(phone){const found=await env.DB.prepare('SELECT id FROM customers WHERE tenant_id=? AND phone=? LIMIT 1').bind(tenantId,phone).first<{id:string}>();customerId=found?.id||null;}if(!customerId){customerId=makeId('cus');await env.DB.prepare('INSERT INTO customers (id,tenant_id,name,phone) VALUES (?,?,?,?)').bind(customerId,tenantId,customerName,phone).run();}const resolved:Array<{id:string;catalogId:string;name:string;qty:number;unit:number;total:number;options:string}>=[];for(const line of lines){const catalogId=String(line.catalog_item_id||''),qty=Math.max(.001,Number(line.qty||1));const product=await env.DB.prepare('SELECT id,name,price_cents FROM catalog_items WHERE id=? AND tenant_id=? AND active=1').bind(catalogId,tenantId).first<{id:string;name:string;price_cents:number}>();if(!product)return json({error:`Item inválido: ${catalogId}`},400);resolved.push({id:makeId('line'),catalogId,name:product.name,qty,unit:product.price_cents,total:Math.round(product.price_cents*qty),options:JSON.stringify(line.options||{})});}const orderId=makeId('ord'),publicCode=`NJ-${Date.now().toString(36).toUpperCase()}`;const subtotal=resolved.reduce((sum,item)=>sum+item.total,0),delivery=cents(input.delivery_cents),discount=Math.min(subtotal+delivery,cents(input.discount_cents)),total=subtotal+delivery-discount;const statements:D1PreparedStatement[]=[env.DB.prepare(`INSERT INTO orders (id,tenant_id,customer_id,public_code,source,transaction_type,status,subtotal_cents,delivery_cents,discount_cents,total_cents,fulfillment_type,notes) VALUES (?,?,?,?,?,?,'new',?,?,?,?,?,?)`).bind(orderId,tenantId,customerId,publicCode,String(input.source||'web'),String(input.transaction_type||'order'),subtotal,delivery,discount,total,String(input.fulfillment_type||'pickup'),input.notes?String(input.notes):null),env.DB.prepare(`INSERT INTO order_events (id,order_id,event_type,to_status,actor_type,actor_id,payload_json) VALUES (?,?,'order.created','new','operator',?,?)`).bind(makeId('evt'),orderId,actor.actorId,JSON.stringify({source:input.source||'web'}))];resolved.forEach(line=>statements.push(env.DB.prepare(`INSERT INTO order_items (id,order_id,catalog_item_id,name,qty,unit_price_cents,total_cents,options_json) VALUES (?,?,?,?,?,?,?,?)`).bind(line.id,orderId,line.catalogId,line.name,line.qty,line.unit,line.total,line.options)));await env.DB.batch(statements);await audit(env,actor,tenantId,'order.create','order',orderId,{public_code:publicCode,total_cents:total});return json({data:{id:orderId,public_code:publicCode,status:'new',total_cents:total}},201);}
const statusRoute=url.pathname.match(/^\/api\/orders\/([^/]+)\/status$/);if(method==='PATCH'&&statusRoute){if(!hasRole(actor,['operator','admin','super_admin']))return json({error:'Permissão insuficiente.'},403);const input=await body(request),nextStatus=String(input.status||'').trim(),id=statusRoute[1];if(!nextStatus)return json({error:'Status obrigatório.'},400);const current=await env.DB.prepare('SELECT status,workflow_id FROM orders WHERE id=? AND tenant_id=?').bind(id,tenantId).first<{status:string;workflow_id:string|null}>();if(!current)return json({error:'Pedido não encontrado.'},404);const valid=await env.DB.prepare(`SELECT ws.step_key FROM workflow_steps ws JOIN workflow_templates wt ON wt.id=ws.workflow_id WHERE wt.tenant_id=? AND ws.step_key=? AND (? IS NULL OR wt.id=?) LIMIT 1`).bind(tenantId,nextStatus,current.workflow_id,current.workflow_id).first<{step_key:string}>();if(!valid)return json({error:'Etapa inválida para o workflow deste tenant.'},400);await env.DB.batch([env.DB.prepare("UPDATE orders SET status=?,updated_at=datetime('now') WHERE id=? AND tenant_id=?").bind(nextStatus,id,tenantId),env.DB.prepare('INSERT INTO order_events (id,order_id,event_type,from_status,to_status,actor_type,actor_id) VALUES (?,?,?,?,?,?,?)').bind(makeId('evt'),id,'order.status.changed',current.status,nextStatus,'operator',actor.actorId)]);await audit(env,actor,tenantId,'order.status.change','order',id,{from:current.status,to:nextStatus});return json({data:{id,from:current.status,status:nextStatus}});}
if(method==='GET'&&url.pathname==='/api/workflows'){const result=await env.DB.prepare(`SELECT wt.id workflow_id,wt.name workflow_name,wt.transaction_type,ws.id step_id,ws.step_key,ws.label,ws.sort_order,ws.color,ws.customer_message FROM workflow_templates wt LEFT JOIN workflow_steps ws ON ws.workflow_id=wt.id WHERE wt.tenant_id=? ORDER BY wt.name,ws.sort_order`).bind(tenantId).all();return json({data:result.results});}
if(method==='GET'&&url.pathname==='/api/automations'){const result=await env.DB.prepare(`SELECT id,name,trigger_type,action_type,active,created_at FROM automation_rules WHERE tenant_id=? ORDER BY created_at DESC`).bind(tenantId).all();return json({data:result.results});}
if(method==='GET'&&url.pathname==='/api/conversations'){const result=await env.DB.prepare(`SELECT cv.id,cv.status,cv.external_id,cv.last_message_at,ch.channel_type,ch.name channel_name,c.name customer_name,c.phone customer_phone FROM conversations cv LEFT JOIN channels ch ON ch.id=cv.channel_id LEFT JOIN customers c ON c.id=cv.customer_id WHERE cv.tenant_id=? ORDER BY COALESCE(cv.last_message_at,cv.created_at) DESC LIMIT 100`).bind(tenantId).all();return json({data:result.results});}
const takeoverRoute=url.pathname.match(/^\/api\/conversations\/([^/]+)\/takeover$/);if(method==='POST'&&takeoverRoute){if(!hasRole(actor,['operator','admin','super_admin']))return json({error:'Permissão insuficiente.'},403);const input=await body(request),mode=input.mode==='ai'?'ai':'human';const result=await env.DB.prepare('UPDATE conversations SET status=? WHERE id=? AND tenant_id=?').bind(mode,takeoverRoute[1],tenantId).run();if(!result.meta.changes)return json({error:'Conversa não encontrada.'},404);await audit(env,actor,tenantId,'conversation.takeover','conversation',takeoverRoute[1],{mode});return json({data:{id:takeoverRoute[1],status:mode}});}return json({error:'Endpoint não encontrado.'},404);}
