/*
 * NegocIAJá! — desenvolvido pela SER Comunicação
 * CNPJ 23.296.513/0001-97
 * Todos os direitos reservados.
 */
const $=s=>document.querySelector(s);async function load(){try{const r=await fetch('/api/platform/operations');const j=await r.json();if(r.status===401){location.href='/login';return}if(!r.ok)throw new Error(j.error||'Falha ao carregar saúde operacional.');const d=j.data;$('#queueReady').textContent=d.queue_binding_ready?'Pronta':'Pendente';$('#queueReady').className=d.queue_binding_ready?'ok':'bad';$('#mpReady').textContent=d.mercadopago_ready?'Pronto':'Pendente';$('#mpReady').className=d.mercadopago_ready?'ok':'bad';$('#jobFailures').textContent=d.recent_job_failures.length;$('#paymentFailures').textContent=d.recent_payment_failures.length;$('#jobs').innerHTML=table(['Status','Total','Última atualização'],d.jobs.map(x=>[x.status,x.total,x.last_update||'—']));$('#failures').innerHTML=table(['Origem','Tenant','Tentativas/Tipo','Erro','Data'],[...d.recent_job_failures.map(x=>['Queue',x.tenant_id,x.attempts,x.last_error||'—',x.updated_at]),...d.recent_payment_failures.map(x=>['Mercado Pago',x.tenant_id||'—',x.event_type,x.error||'—',x.created_at])]);$('#channels').innerHTML=table(['Tipo','Status','Total'],d.channels.map(x=>[x.channel_type,x.status,x.total]))+table(['Assinatura','Total'],d.subscriptions.map(x=>[x.status,x.total]));}catch(e){$('#opsStatus').textContent=e.message||'Erro.'}}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function table(head,rows){if(!rows.length)return'<p>Nenhum registro.</p>';return`<table><thead><tr>${head.map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table>`}load();