/* NegocIAJá! — desenvolvido pela SER Comunicação */
const list=document.querySelector('#conversationList'),messages=document.querySelector('#messages'),nameEl=document.querySelector('#chatName'),channelEl=document.querySelector('#chatChannel'),modeEl=document.querySelector('#chatMode'),take=document.querySelector('#takeoverBtn'),release=document.querySelector('#releaseBtn'),closeBtn=document.querySelector('#closeBtn'),composer=document.querySelector('#composer'),input=document.querySelector('#messageInput'),send=document.querySelector('#sendBtn');let current=null,items=[];
async function api(path,opt={}){const r=await fetch(path,{headers:{'content-type':'application/json',...(opt.headers||{})},...opt});let j={};try{j=await r.json()}catch{}if(r.status===401){location.href='/login';throw new Error('Sessão expirada.')}if(!r.ok)throw new Error(j.error||'Erro');return j.data}
function el(tag,text,cls){const x=document.createElement(tag);if(text!==undefined)x.textContent=text;if(cls)x.className=cls;return x}
async function loadList(){list.innerHTML='';try{items=await api('/api/conversations?status=open');if(!items.length){list.append(el('div','Nenhuma conversa aberta.','empty'));return;}items.forEach(c=>{const b=el('button',undefined,'conversation-item');b.type='button';b.dataset.id=c.id;b.append(el('strong',c.customer_name||c.customer_address||'Cliente'));b.append(el('span',`${c.channel} • ${c.mode==='human'?'atendimento humano':'IA'}`));b.append(el('small',new Date(c.last_message_at).toLocaleString('pt-BR')));b.addEventListener('click',()=>openConversation(c.id));list.append(b)});}catch(e){list.append(el('div',e.message,'empty'))}}
function mediaNode(m){if(!m.media_key)return null;const url=`/api/conversation-media/${encodeURIComponent(m.id)}`;if(m.message_type==='image'){const x=document.createElement('img');x.src=url;x.alt='Imagem recebida';x.loading='lazy';x.style.maxWidth='100%';x.style.borderRadius='12px';return x}if(m.message_type==='audio'){const x=document.createElement('audio');x.src=url;x.controls=true;x.preload='metadata';return x}if(m.message_type==='video'){const x=document.createElement('video');x.src=url;x.controls=true;x.preload='metadata';x.style.maxWidth='100%';x.style.borderRadius='12px';return x}const a=el('a','📎 Abrir documento','media-link');a.href=url;a.target='_blank';a.rel='noopener';return a}
function renderMessage(m){const b=el('div',undefined,`bubble ${m.direction==='outbound'?'outbound':''}`);if(m.text_content)b.append(el('div',m.text_content));const media=mediaNode(m);if(media)b.append(media);if(!m.text_content&&!media)b.append(el('div',`[${m.message_type||'mensagem'}]`));b.append(el('small',`${m.sender_type} • ${new Date(m.created_at).toLocaleString('pt-BR')}`));messages.append(b)}
async function openConversation(id){current=id;document.querySelectorAll('.conversation-item').forEach(x=>x.classList.toggle('active',x.dataset.id===id));messages.innerHTML='';try{const data=await api(`/api/conversations/${id}`);const base=items.find(x=>x.id===id)||{};nameEl.textContent=base.customer_name||base.customer_address||'Cliente';channelEl.textContent=base.channel||'';modeEl.textContent=data.conversation.mode==='human'?'Humano':'IA';data.messages.forEach(renderMessage);messages.scrollTop=messages.scrollHeight;const human=data.conversation.mode==='human';take.hidden=human;release.hidden=!human;closeBtn.hidden=false;input.disabled=false;send.disabled=false;}catch(e){messages.append(el('div',e.message,'empty-chat'))}}
async function action(kind){if(!current)return;try{await api(`/api/conversations/${current}/${kind}`,{method:'POST',body:'{}'});await loadList();await openConversation(current)}catch(e){alert(e.message)}}
take.addEventListener('click',()=>action('takeover'));release.addEventListener('click',()=>action('release'));closeBtn.addEventListener('click',async()=>{await action('close');current=null;nameEl.textContent='Selecione uma conversa';channelEl.textContent='';modeEl.textContent='';messages.innerHTML='<div class="empty-chat">Conversa encerrada.</div>';take.hidden=release.hidden=closeBtn.hidden=true;input.disabled=send.disabled=true;});composer.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!current||!text)return;send.disabled=true;try{await api(`/api/conversations/${current}/messages`,{method:'POST',body:JSON.stringify({text})});input.value='';await openConversation(current);await loadList()}catch(err){alert(err.message)}finally{send.disabled=false}});document.querySelector('#refreshInbox').addEventListener('click',loadList);loadList();
