const form=document.querySelector('#loginForm');const errorBox=document.querySelector('#loginError');const button=document.querySelector('#loginBtn');const platform=document.querySelector('#platformLogin');const tenantInput=form.elements.tenant_slug;platform.addEventListener('change',()=>{tenantInput.disabled=platform.checked;if(platform.checked)tenantInput.value=''});form.addEventListener('submit',async e=>{e.preventDefault();errorBox.textContent='';button.disabled=true;button.textContent='Entrando…';const data=new FormData(form);try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:data.get('email'),password:data.get('password'),tenant_slug:platform.checked?'':data.get('tenant_slug')})});const payload=await response.json();if(!response.ok)throw new Error(payload.error||'Não foi possível entrar.');location.href='/app'}catch(err){errorBox.textContent=err.message||'Não foi possível entrar.';button.disabled=false;button.textContent='Entrar no NegocIAJá!'}});