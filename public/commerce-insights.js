/* NegocIAJá! — desenvolvido pela SER Comunicação | CNPJ 23.296.513/0001-97 | Todos os direitos reservados. */
const $=s=>document.querySelector(s);const money=c=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format((Number(c)||0)/100);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function table(head,rows){if(!rows.length)return'<p>Nenhum registro no período.</p>';return`<table><thead><tr>${head.map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table>`}async function load(){const r=await fetch('/api/commerce/insights');const j=await r.json();if(r.status===401){location.href='/login';return}if(!r.ok){document.body.insertAdjacentHTML('beforeend',`<p>${esc(j.error||'Falha ao carregar')}</p>`);return}const d=j.data;$('#sessions').textContent=d.sessions;$('#buyers').textContent=d.buyer_sessions;$('#conversion').textContent=`${(Number(d.conversion_rate||0)*100).toFixed(1)}%`;$('#orders').textContent=d.orders;$('#repeats').textContent=d.repeats;$('#revenue').textContent=money(d.revenue_cents);$('#abandoned').innerHTML=table(['Cliente','Contato','Itens','Valor','Última atividade'],d.abandoned_carts.map(x=>[x.customer_name||'Não identificado',x.customer_phone||x.customer_email||'—',x.item_count,money(x.cart_value_cents),new Date(x.updated_at).toLocaleString('pt-BR')]));$('#events').innerHTML=table(['Evento','Total'],d.events.map(x=>[x.event_type,x.total]));}load();